% Plantilla version 07 - 12 - 2023
% José y Rocío

%%% PAQUETES %%%
\RequirePackage{commands}
\usepackage[x11names, dvipsnames]{xcolor}

% ======= General ========
\RequirePackage[utf8]{inputenc}
\RequirePackage[english]{babel}
\RequirePackage[indentafter]{titlesec}
\RequirePackage{framed}

% Utilidades
\RequirePackage{todonotes}
\RequirePackage[shortlabels]{enumitem}
\RequirePackage{comment}
\RequirePackage{hyperref}
\RequirePackage{calc}
\RequirePackage{xpatch}
\RequirePackage{longtable}

% Magia de enumerate                                    by Pololo
\setlist[enumerate,1]{label=\arabic*), ref=\arabic*}

% Graficos
\RequirePackage{graphicx}
\RequirePackage[many]{tcolorbox}
\RequirePackage{color}
\RequirePackage{float}
\RequirePackage{tikz-cd}
\RequirePackage{bmpsize}
\DeclareGraphicsExtensions{.bmp,.png,.pdf,.jpg}
\usetikzlibrary{babel}

%%% DISEÑO DEMOSTRACIONES %%%%

\makeatletter
\renewenvironment{proof}[1][\proofname]{%
  \par\pushQED{\qed}\normalfont%
  \topsep6\p@\@plus6\p@\relax
  \trivlist\item[\hskip\labelsep\itshape#1\@addpunct{.}]%
  \ignorespaces
}{%
  \popQED\endtrivlist\@endpefalse
}
\makeatother
\newenvironment{sketch}{\begin{proof}[Bosquejo]}{\end{proof}}
\newenvironment{solution}{\begin{proof}[{\color{rosaclaro}Soluci\'on}]}{\end{proof}}
\newenvironment{Proof}[1]{\begin{proof}[Demostración (#1)]}{\end{proof}}
\newenvironment{hint}{\begin{proof}[Pista]}{\end{proof}}
\newenvironment{sol}{\begin{proof}[Solución]}{\end{proof}}

%%% DISEÑO BIBLIOGRAFÍA %%%%

\RequirePackage{csquotes}
\RequirePackage[
	backend=biber,
	doi=true,
	uniquename=init,
	style=nature,
	sorting=nyt,
	defernumbers=true,
	citestyle=numeric]{biblatex}
\renewcommand{\mkbibnamefamily}[1]{\textsc{#1}} % nombres de los autores en versallitas

% Citas en negritas
\DeclareFieldFormat{labelnumber}{\mkbibbold{#1}}
\AtBeginBibliography{
	\DeclareFieldFormat{labelnumber}{#1}
}
\addbibresource{reference.bib} % archivo de referencias
\renewcommand*{\bibfont}{\small} % bibliografías más pequeñas

%itbf
\newcommand{\ib}[1]{\textbf{\textit{#1}}}
\renewcommand{\strong}[1]{{\bfseries\itshape #1}}
\newcommand{\citea}[2][]{\citeauthor{#2}~\cite[#1]{#2}}
\newcommand{\citet}[2][]{\citea[#1]{#2}~(\citeyear{#2})}

%CORTAR LAS COSITAS
\allowdisplaybreaks


